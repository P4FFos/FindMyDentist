services:
  nginx:
    image: nginx:latest
    container_name: nginx-load-balancer
    ports:
      - "8080:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - mqtt-network

networks:
  mqtt-network:
    external: true
